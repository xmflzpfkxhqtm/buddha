interface TopNavProps {
  className?: string;
}

export default function TopNav({ className }: TopNavProps) {
  return (
    <nav className={className}>
      // ... existing code ...
    </nav>
  );
} 